The sidebyside_raw_data folder contains the side-by-side stereo images captured by the camera.

The MC_Stereo_depth folder contains the depth maps generated using MC-Stereo (https://github.com/MiaoJieF/MC-Stereo).

The SGM_depth folder contains the depth maps generated using the Semi-Global Matching (SGM) algorithm.