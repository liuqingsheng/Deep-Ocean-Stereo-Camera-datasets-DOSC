The `Algorithm` folder includes the implementation of the proposed DGRC algorithm, together with the images used in the paper.

The `DOSC_Dataset` folder provides representative images collected by the DOSC camera in deep-sea environments, including natural seafloor scenes and artificial structures.
